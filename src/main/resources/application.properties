spring.application.name=demo
spring.profiles.active=dev

# JPA / Hibernate
spring.jpa.open-in-view=false

# Stable page JSON serialization (wraps Page in PagedModel)
spring.data.web.pageable.serialization-mode=via-dto

# RFC 9457 ProblemDetail error responses for REST API
spring.mvc.problemdetails.enabled=true

# OpenAPI / Swagger UI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operations-sorter=method

# Cache busting: append content hash to static resource URLs (e.g. utils.js?v=abc123)
spring.web.resources.chain.strategy.content.enabled=true
spring.web.resources.chain.strategy.content.paths=/**

# Actuator — expose health and info endpoints
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.info.env.enabled=true
info.app.name=${spring.application.name}
info.app.java=${java.version}
info.app.spring-boot=${spring-boot.version}
