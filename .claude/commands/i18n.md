---
description: Run the i18n-checker agent on the given scope
---

Use the `i18n-checker` agent to scan for hardcoded user-facing strings in:

$ARGUMENTS

If no scope was given, ask the user: file, directory, or "recent changes" (`git diff main...HEAD --name-only`).

Report findings with suggested message keys. Ask the user before adding keys to `messages.properties` or editing source files.
